{
  "name": "charliecheesman.net",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "serve": "npx serve .",
    "agent:sync": "node scripts/sync-agent.js"
  },
  "devDependencies": {
    "marked": "^12.0.0"
  }
}
